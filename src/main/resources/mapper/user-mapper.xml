<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.UserMapper">
    <sql id="selectFields">
        id,username,password,salt,email,type,status,activationCode,headerUrl,creatTime
    </sql>
    <sql id="insertFields">
        username,password,salt,email,type,status,activationCode,headerUrl,creatTime
    </sql>
    <resultMap type="User" id="User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="salt" property="salt"/>
        <result column="email" property="email"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="activationCode" property="activationCode"/>
        <result column="headerUrl" property="headerUrl"/>
        <result column="creatTime" property="creatTime"/>
    </resultMap>
    <select id="selectByid" parameterType="int" resultMap="User">
        select <include refid="selectFields" />
        from user where id=#{uid}
    </select>
    <select id="selectByName" parameterType="String" resultMap="User">
        select <include refid="selectFields" />
        from user where username=#{name}
    </select>
    <select id="selectByEmail" parameterType="String" resultMap="User">
        select <include refid="selectFields" />
        from user where email=#{email}
    </select>
    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into user(<include refid="insertFields"/>)
                                values(#{username},#{password},#{salt},#{email}
                                ,#{type},#{status},#{activationCode},#{headerUrl}
                                ,#{creatTime})
    </insert>
    <update id="updataStatus"  >
        update user
        set status=#{status}
        where id=#{uid}
    </update>
    <update id="updataHeardeUrl"  >
        update user
        set headerUrl=#{headerUrl}
        where id=#{uid}
    </update>
    <update id="updataPassWord"  >
        update user
        set password=#{passWord}
        where id=#{uid}
    </update>

</mapper>